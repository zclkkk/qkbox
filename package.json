{
  "name": "qkbox",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "check": "npm run frontend:check && npm run frontend:build && npm run go:test && npm run go:vet && wails3 doctor",
    "test": "npm run go:test",
    "build": "npm run build:qkbox && npm run build:qkbox-provider && npm run build:desktop",
    "go:fmt": "gofmt -w apps cmd core internal platform shared",
    "go:test": "go test -tags with_clash_api ./...",
    "go:vet": "go vet -tags with_clash_api ./...",
    "frontend:install": "npm --prefix apps/desktop/frontend install",
    "frontend:check": "wails3 task -dir apps/desktop generate:bindings && npm --prefix apps/desktop/frontend run check",
    "frontend:build": "wails3 task -dir apps/desktop build:frontend",
    "build:qkbox": "wails3 task build:qkbox && wails3 task build:qkbox:win",
    "build:qkbox-provider": "wails3 task build:qkbox-provider",
    "build:desktop": "wails3 task -dir apps/desktop build",
    "package:windows": "wails3 task package:windows:nsis",
    "package:linux": "wails3 task package:linux:deb",
    "package:macos": "wails3 task package:macos:dmg",
    "release:verify": "node packaging/verify-release.mjs"
  }
}
